# order-validation
Neste repositório iremos ver algumas maneiras de fazer validação, como por exemplo, validação no domain por interfaces, validação no domain para valores vindo do DB e validação no controller (borda).
